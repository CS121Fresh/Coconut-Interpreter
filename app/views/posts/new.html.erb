  <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title>Editor</title>
  <style type="text/css" media="screen">
    body {
        overflow: hidden;
    }
    #editor {
        margin: 0;
        position: absolute;
        top: 0;
        bottom: 0;
        left: 0;
        right: 0;
    }
  </style>
</head>
<body>

<pre id="editor">def foo(items) {
    print "Hello World" 
}</pre>

<script src="src-noconflict/ace.js" type="text/python" charset="utf-8"></script>
<script>
    var editor = ace.edit("editor");
    editor.setTheme("ace/theme/twilight");
    editor.session.setMode("ace/mode/python");
    function produceMessage(){
        var msg= editor.getValue(); // or session.getValue;
        return msg;
    }
    document.write(produceMessage());
</script>


</body>
</html>







<!--
<h1>New Code</h1>

<p>This page needs to be decorated so it looks like a console</p>
<p>We also need to add a frame for returning output, so that people</p>
<p>We will make this page to our future home page, and possibly deleteing all other links, only keeping one to show the past code implemented if needed</p>

<%= render 'form', post: @post %>

<p>After creating this goes to show page</p>

<%= link_to 'check exisiting code', posts_path %> -->
